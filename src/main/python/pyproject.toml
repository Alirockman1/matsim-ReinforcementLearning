[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "python_matsim_withinday"
version = "0.1.0"

[tool.setuptools.packages.find]
include = ["project*", "withinday*"]
exclude = ["backup*"]